# AITicket 仓库血缘与发布规则

执行跨仓移植、分支同步、Mini/172 发布、认证数据库或密钥变更、用户批量变更、发布验收前，必须先完整读取 `docs/standards/repository-lineage-and-release.md`，并以它作为权威规范。

发现仓库血缘、远端、运行控制面或环境现状已漂移时，必须在同一变更中同步更新 aiticket 与 aiticket-deployable 两份规范镜像，并更新跨 Agent 记忆索引。禁止在本规则文件中复制完整规范。
