{
    "name": "ai-ticket-e2e-tests",
    "version": "1.0.0",
    "description": "E2E tests for AI Ticket system using Playwright",
    "scripts": {
        "test": "npx playwright test",
        "test:ui": "npx playwright test --ui",
        "test:headed": "npx playwright test --headed"
    },
    "devDependencies": {
        "@playwright/test": "^1.58.1"
    }
}
